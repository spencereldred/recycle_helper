<!DOCTYPE html>
<html ng-app="Hi5Exchange">
<head>
  <title>Recycle</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

  <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"></script>
  <script src="https://maps.googleapis.com/maps/api/js?v=3.exp&sensor=false"></script>

</head>
<body>
  <div id="wrap">
  <header>
    <div class="bs">
      <% if !current_user %>
        <%= render :partial => "layouts/signin" %>
        <%= render :partial => "layouts/redeemer_signup" %>
        <%= render :partial => "layouts/recycler_signup" %>
      <% end %>
      <%# if !current_user || !current_user[:id] %>
        <%#= link_to "Home", root_path %>
      <%# end %>
      <% if current_user %>
        <img id="logo" src="/assets/hi5_small.png" class="img-responsive" style="width: 5%;"alt="HI5 logo">
        <% if current_user.function == "redeemer" || current_user.function == "recycler" ||    current_user.function == "admin" %>
          <%= render :partial => "layouts/profile" %>
        <% end %>
        <%# if current_user.function == "redeemer" || current_user.function == "admin" %>
          <!-- <a href="/redeemers"><button class="btn btn-default btn-lg">Select</button></a> -->
        <%# end %>
        <%# if current_user.function == "recycler" || current_user.function == "admin" %>
          <!-- <a href="/transactions"><button class="btn btn-default btn-lg">Recycle</button></a> -->
        <%# end %>
      <% end %>
      <% if current_user %>
        <a href="/logout"><button class="btn btn-default btn-lg">Logout</button></a>
      <% end %>
    </div>
  </header>
  <div class="container">
    <div class="row">

      <div class="col-sm-offset-4 col-sm-8 error">
        <% if flash[:errors] %>
          <% flash[:errors].each do |error| %>
            <li><%= error %></li>
          <% end %>
        <% end %>
     </div>
      <%= yield %>
    </div>
  </div>
</div>
<div class="row">
  <%= render  :partial => "layouts/footer" %>
</div>
</body>
</html>

